import { Injectable } from '@angular/core';
//import { Cell } from '../classes/cell';
//import { grid } from '../data/mock-grid';

@Injectable()
export class GridService {
    getCells(): void {//Promise<Cell[][]> {
        //return Promise.resolve(grid);
    }
}
